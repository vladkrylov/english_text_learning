// Generated by CoffeeScript 1.8.0
(function() {
  var $;

  $ = jQuery;

  $(document).ready(function() {
    var phraseField;
    phraseField = $("#phrase");
    $(".choosable").click(function() {
      var oldValue;
      oldValue = phraseField.val();
      if (oldValue !== "") {
        oldValue += " ";
      }
      return phraseField.val(oldValue + $(this).text().replace(/\s+/g, '').replace(/[\.,\/#!?$%\^&\*;:{}=\_"`~()]/g, ""));
    });
    $("#clearphrase").click(function() {
      return phraseField.val("");
    });
    return $("#submit_phrase").click(function() {
      var resp;
      resp = $("#addphrase_response");
      return $.ajax({
        url: '/ajax_add_phrase',
        type: 'POST',
        data: {
          phrase: $('#phrase').val(),
          tense: $('#tense').val(),
          url: location.href
        },
        success: function(data, textStatus, jqXHR) {
          var json;
          json = JSON.parse(data);
          resp.empty();
          resp.append(json["statustext"]);
          if (resp.hasClass("alert-danger") === true) {
            resp.removeClass("alert-danger");
          }
          if (resp.hasClass("alert-success") === true) {
            resp.removeClass("alert-success");
          }
          if (resp.hasClass("alert-warning") === true) {
            resp.removeClass("alert-warning");
          }
          if (json["status"] === "already_exists") {
            resp.addClass("alert-warning");
          } else {
            resp.addClass("alert-success");
            $('#phrase').val("");
          }
          return resp.hide().fadeIn(500).delay(2000).fadeOut(1500);
        },
        error: function(jqXHR, textStatus, errorThrown) {
          resp.empty();
          resp.append("Error");
          if (resp.hasClass("alert-success") === true) {
            resp.removeClass("alert-success");
          }
          if (resp.hasClass("alert-warning") === true) {
            resp.removeClass("alert-warning");
          }
          return resp.addClass("alert-danger");
        }
      });
    });
  });

}).call(this);
